<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Ignored on Android 10; required from API 34, where a typed foreground service
         must hold the permission matching its type. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
      Android 10 refuses to let a background app start an activity, which is how the alarm and
      timer commands hand off to the clock app. Holding this permission is the documented
      exemption; on this device it is granted because the apk is installed as a system app.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      android:persistent only takes effect once the apk is installed under /system/priv-app, which
      is how this device is provisioned. On an ordinary install the platform ignores it and the
      foreground service alone keeps the process alive.
    -->
    <application
        android:allowBackup="false"
        android:icon="@drawable/alf_logo"
        android:label="@string/app_name"
        android:persistent="true"
        android:roundIcon="@drawable/alf_logo"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          dataSync rather than microphone: the microphone foreground service type only exists from
          API 30, and this device runs Android 10.
        -->
        <service
            android:name=".AlfService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
